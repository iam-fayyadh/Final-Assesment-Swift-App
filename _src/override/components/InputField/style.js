import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginTop: 10,
  },
  bottomContainer: {
    marginTop: 10,
    marginBottom: 10,
  },
  listContainer: {
    marginTop: 10,
  },
});

export default styles;
